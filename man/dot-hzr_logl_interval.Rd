% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/likelihood-multiphase.R
\name{.hzr_logl_interval}
\alias{.hzr_logl_interval}
\title{Interval-censored log-likelihood contribution}
\usage{
.hzr_logl_interval(
  cumhaz_lower,
  cumhaz_upper,
  lower,
  upper,
  weights,
  objective = c("likelihood", "sas")
)
}
\arguments{
\item{cumhaz_lower}{Cumulative hazard at the interval lower bounds,
\eqn{\Lambda(l)}.}

\item{cumhaz_upper}{Cumulative hazard at the interval upper bounds,
\eqn{\Lambda(u)}.}

\item{lower}{Interval lower bounds. Used only by \code{objective = "sas"}.}

\item{upper}{Interval upper bounds. Used only by \code{objective = "sas"}.}

\item{weights}{Case weights. In a SAS parity run these are the ICENSOR
variable, which is a weight (a death count in an aggregated study), not
merely an indicator.}

\item{objective}{\code{"likelihood"} for the interval probability -- the default,
and the only statistically consistent form -- or \code{"sas"} for the
interval-mean-hazard density term \verb{PROC HAZARD} accumulates. See
\code{inst/dev/SAS-INTERVAL-OBJECTIVE-DESIGN.md}.}
}
\value{
Scalar summed contribution; \code{-Inf} for infeasible parameters.
}
\description{
The single place the interval-censored contribution is written.  Both
\code{.hzr_logl_multiphase()} and the finite-difference closure inside
\code{.hzr_gradient_multiphase()} delegate here, so the optimiser cannot step by
the gradient of a different objective than the one it evaluates.
}
\details{
Callers pass \strong{only the interval rows} -- already subset by \code{status == 2}
-- so this helper never sees the status mask and cannot disagree with a
caller about which rows are intervals.
}
\keyword{internal}
