% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/likelihood-multiphase.R
\name{.hzr_check_sas_status}
\alias{.hzr_check_sas_status}
\title{Reject row types the SAS objective has no counterpart for}
\usage{
.hzr_check_sas_status(status, objective)
}
\arguments{
\item{status}{Numeric event indicator.}

\item{objective}{Resolved objective, \code{"likelihood"} or \code{"sas"}.}
}
\value{
\code{NULL}, invisibly; called for its side effect.
}
\description{
\verb{PROC HAZARD} has no left-censoring statement, so no SAS run corresponds to
a fit containing left-censored rows.  That is a data defect rather than
parameter infeasibility, so it stops rather than returning \code{-Inf}.
}
\details{
Called from BOTH \code{.hzr_logl_multiphase()} and \code{.hzr_gradient_multiphase()}.
Guarding only the objective would leave the gradient computing happily for
data the objective refuses -- and the gradient is reachable on its own, for
instance from \code{.hzr_score_test()}, so the objective's refusal is not
guaranteed to come first.
}
\keyword{internal}
