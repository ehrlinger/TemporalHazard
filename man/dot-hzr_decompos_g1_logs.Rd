% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/decomposition.R
\name{.hzr_decompos_g1_logs}
\alias{.hzr_decompos_g1_logs}
\title{Log-scale terms shared by the m > 0 branches of \code{\link[=hzr_decompos]{hzr_decompos()}}}
\usage{
.hzr_decompos_g1_logs(time, t_half, nu, m)
}
\arguments{
\item{time}{Numeric vector of positive times.}

\item{t_half}{Positive scalar.}

\item{nu}{Nonzero scalar.}

\item{m}{Positive scalar.}
}
\value{
List with \code{log_S} (\eqn{= -\log(\mathrm{btnu})/m}) and \code{log_g}.
}
\description{
Cases 1 (\code{nu > 0}) and 3 (\code{nu < 0}) evaluate the same intermediate
quantities; only the sign of \code{rho} and the final assembly of \code{G} differ.
Forming them directly overflows well inside the range an optimizer can
reach: \code{2^m} is \code{Inf} from \code{m = 1024}, and \code{bt^(-1/nu)} overflows sooner
still (from \code{m ~ 750} at \code{t/t_half = 0.5}, \code{m*nu = 3}).  \code{btnu} then
becomes \code{Inf} and \code{btnu^(-1/m)} collapses silently to 0.
}
\details{
The \code{m} in \code{rho}'s \code{(2^m - 1)/m} factor cancels the explicit \code{m}
multiplier, so exactly
\deqn{m \, b(t)^{-1/\nu} = (t_{1/2}/t)^{1/\nu} (2^m - 1),}
and \code{log(btnu)} follows from a softplus of the log of that product.  Every
intermediate below stays finite for any \code{m > 0} a double can hold.
}
\keyword{internal}
