% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/phase-spec.R
\name{hzr_theta_names}
\alias{hzr_theta_names}
\title{Parameter names for a phase specification, in \code{theta} order}
\usage{
hzr_theta_names(phases, covariates = NULL)
}
\arguments{
\item{phases}{A non-empty list of \code{\link[=hzr_phase]{hzr_phase()}} objects, as passed to
\code{\link[=hazard]{hazard()}}.}

\item{covariates}{Optional named list mapping phase name to that phase's
covariate column names, e.g. \code{list(early = c("age", "sex"))}.  Phases
absent from the list are treated as having no covariates.  Names are used
verbatim, so they must match the columns the fit will see.}
}
\value{
A character vector whose \strong{order is the required \code{theta} order}.
Its length is the number of parameters the specification implies, so
\code{length(hzr_theta_names(phases))} is the length \code{theta} must have.
}
\description{
Returns the names of the \code{theta} vector a multiphase \code{\link[=hazard]{hazard()}} fit builds
from \code{phases}, in the order \code{theta} requires.  Use it to check a
hand-written starting vector against the specification it is meant to go
with, before any fit runs.
}
\details{
\code{theta} is positional and its entries are not on a common scale.  For an
\code{early} + \code{late} pair the layout is

\if{html}{\out{<div class="sourceCode">}}\preformatted{early.log_mu, early.log_t_half, early.nu, early.m,
late.log_mu,  late.log_tau,     late.gamma, late.alpha, late.eta
}\if{html}{\out{</div>}}

so the late phase logs \code{mu} and \code{tau} while carrying \code{gamma}, \code{alpha} and
\code{eta} on the natural scale.  \strong{Wrapping the wrong element in \code{log()}
produces a fit, not an error}, which is why a comment describing the order
is not enough and this returns the real thing.  The order is a property of
the specification, so it changes the moment a phase is added, removed or
retyped.

Phase names come from \code{names(phases)}; unnamed phases are labelled
\code{phase_1}, \code{phase_2}, ... by the same validation \code{\link[=hazard]{hazard()}} applies, so the
labels here are the labels a fit will use.

Phase names must be unique, and \code{"total"} is reserved; this applies
the same validation \code{\link[=hazard]{hazard()}} does, so both reject it identically.
}
\examples{
phases <- list(early = hzr_phase("cdf"), late = hzr_phase("g3"))
hzr_theta_names(phases)

# Check a hand-written starting vector before fitting.
theta0 <- c(log(0.05), log(0.2), 0, -0.4, log(0.03), log(1), 1, 1, 1)
stopifnot(length(theta0) == length(hzr_theta_names(phases)))
setNames(theta0, hzr_theta_names(phases))

# With covariates on one phase.
hzr_theta_names(phases, covariates = list(early = c("age", "sex")))

}
\seealso{
\code{\link[=hzr_phase]{hzr_phase()}} for building a specification, \code{\link[=hazard]{hazard()}} for the fit
whose \code{theta} this names.
}
